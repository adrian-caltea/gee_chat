# Copy this to .env and fill values for local development
GEMINI_API_KEY=
# Optionally set a different model
GEMINI_MODEL=gemini-2.0-flash-lite
# Use dev/mock LLM if you don't want to call the real API
# USE_DEV_LLM=1
